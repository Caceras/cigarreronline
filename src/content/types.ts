export type { Product, Brand, Guide, Collection, Filter, Section, Faq } from '../../content/schema';
import type { STRENGTHS } from '../../content/schema';
export type Strength = (typeof STRENGTHS)[number];
